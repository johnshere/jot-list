export const isRouteOrMenu = async (_: string) => {
    // const permission = await findPermission(path)
    // return permission && !PermissionTypeEnum.BUTTON.isEqual(permission.type)
    return true
}
