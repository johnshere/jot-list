const Gateway = '/gateway'
const AiAssist = `${Gateway}/ai-assist`
const TextCompare = `${Gateway}/textcompare`
const Eval = `${Gateway}/evaltool`
const Supplier = `${Gateway}/supplierlibrary`
const SupplyManage = `/supplymanage-web`
const Upms = `${Gateway}/yzc-upms`
const Cportal = `/server/cportal/cportal-api/`
const Deposit = `${Gateway}/deposit`
const Order = `/order-api/web`
const Contract = `${Gateway}/contractmanagement`
const Smallfile = `${Gateway}/yzc-smallfile`
const Bigfile = `${Gateway}/yzc-bigfile`
const Bidding = `${Gateway}/supply-bidding`

export const Base = {
    Gateway,
    Bidding,
    Contract,
    AiAssist,
    Eval,
    Supplier,
    SupplyManage,
    Upms,
    TextCompare,
    Cportal,
    Deposit,
    Order,
    Smallfile,
    Bigfile
}
